; ============================================================
; KeyBridge  v5.0
; Bluetooth keyboard → Wyse 50 scan matrix emulation
; ============================================================

; Active environment: original ESP32 with Classic BT + BLE + WiFi
[env:esp32dev]
platform = espressif32@^6.9.0
board = esp32dev
framework = arduino, espidf

board_build.partitions = partitions.csv   ; 3MB app, no OTA (BT+WiFi needs >1MB)

lib_deps =
    bblanchon/ArduinoJson@~7.4.0

monitor_speed = 115200

; ---------- ESP32-S3 (USB host, BLE only — currently unused) ----------
; [env:esp32s3]
; platform = espressif32@^6.9.0
; board = esp32-s3-devkitc-1
; framework = arduino, espidf
; board_build.arduino.usb_mode = 0
; board_build.arduino.cdc_on_boot = 0
; board_build.partitions = partitions.csv
; lib_deps =
;     bblanchon/ArduinoJson@~7.4.0
; monitor_speed = 115200
